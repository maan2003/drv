[package]
name = "userspace-vfio"
description = "Fail-closed Linux VFIO cdev and iommufd ownership primitives"
edition.workspace = true
license.workspace = true
version.workspace = true

[features]
test-support = []

[dependencies]
rustix = { version = "=1.1.4", features = ["event", "time"] }
