[package]
name = "qrtr-socket"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Safe owned Linux AF_QIPCRTR datagram sockets and service discovery"

[dependencies]
libc = "0.2"

