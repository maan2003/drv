diff -ruN a/src/connectivity/wlan/lib/mlme/rust/host_tests/client_gate.rs b/src/connectivity/wlan/lib/mlme/rust/host_tests/client_gate.rs
--- a/src/connectivity/wlan/lib/mlme/rust/host_tests/client_gate.rs	2026-08-11 16:02:35.843202660 +0000
+++ b/src/connectivity/wlan/lib/mlme/rust/host_tests/client_gate.rs	2026-08-11 16:02:35.838483502 +0000
@@ -182,6 +182,7 @@
     link_up: bool,
     events: Vec<fidl_mlme::MlmeEvent>,
     order: Vec<&'static str>,
+    tx_delay: std::time::Duration,
 }
 
 struct FakeDeviceOps {
@@ -271,6 +272,8 @@
         tx_flags: fidl_softmac::WlanTxInfoFlags,
         _async_id: Option<fuchsia_trace::Id>,
     ) -> Result<(), zx::Status> {
+        let tx_delay = self.effects.lock().unwrap().tx_delay;
+        std::thread::sleep(tx_delay);
         self.effects.lock().unwrap().frames.push((buffer.to_vec(), tx_flags));
         Ok(())
     }
@@ -615,11 +618,17 @@
             }
         }
         let stale_sme_timer = stale_sme_timer.expect("SME SAE retry timer");
-        let stale_mlme_timer = mlme_timers.try_recv().expect("MLME connect timer");
+        let original_mlme_timer = mlme_timers.try_recv().expect("original MLME connect timer");
         assert!(matches!(
-            stale_mlme_timer.1.event,
+            original_mlme_timer.1.event,
             wlan_mlme::client::TimedEvent::Connecting
         ));
+        let fired: Vec<_> = wlan_mlme::common::timer::make_async_timed_event_stream(
+            futures::stream::iter([original_mlme_timer]),
+        )
+        .collect()
+        .await;
+        assert!(fired.is_empty(), "SAE TX must cancel the original MLME timer");
 
         let mut peer_updates = vec![];
         peer.on_sae_frame_rx(&mut peer_updates, sae_tx).unwrap();
@@ -747,6 +756,11 @@
         }
         assert!(saw_eapol_conf && saw_key_conf, "MLME confirmations must return to SME");
 
+        let mut stale_mlme_timer = mlme_timers.try_recv().expect("renewed MLME connect timer");
+        while let Ok(timer) = mlme_timers.try_recv() {
+            stale_mlme_timer = timer;
+        }
+
         sme.on_disconnect_command(
             fidl_sme::UserDisconnectReason::WlanSmeUnitTesting,
             Default::default(),
@@ -778,3 +792,70 @@
         assert!(!effects.link_up, "cancellation must leave the controlled port closed");
         });
 }
+
+#[test]
+fn sae_tx_renews_connect_timer_before_delayed_status77() {
+    tokio::runtime::Builder::new_current_thread()
+        .enable_time()
+        .build()
+        .unwrap()
+        .block_on(async {
+            let (device, effects) = FakeDeviceOps::new();
+            let (timer, mut timers) = wlan_mlme::common::timer::create_timer();
+            let mut mlme = ClientMlme::new(Default::default(), device, timer).await.unwrap();
+            let mut bss = wpa3_bss();
+            bss.beacon_period = 1;
+            mlme.handle_mlme_request(MlmeRequest::Connect(fidl_mlme::ConnectRequest {
+                selected_bss: bss,
+                connect_failure_timeout: 10,
+                auth_type: fidl_mlme::AuthenticationTypes::Sae,
+                sae_password: vec![],
+                wep_key: None,
+                security_ie: vec![],
+                owe_public_key: None,
+            }))
+            .await
+            .unwrap();
+            assert_eq!(mlme.sae_state_name(), "authenticating");
+            let original = timers.try_recv().expect("original connect timer");
+
+            effects.lock().unwrap().tx_delay = std::time::Duration::from_millis(20);
+            mlme.handle_mlme_request(MlmeRequest::SaeFrameTx(fidl_mlme::SaeFrame {
+                peer_sta_address: AP,
+                seq_num: 1,
+                status_code: fidl_ieee80211::StatusCode::Success,
+                sae_fields: vec![20, 0],
+            }))
+            .await
+            .unwrap();
+            assert_eq!(mlme.sae_state_name(), "authenticating");
+            let refreshed = timers.try_recv().expect("refreshed connect timer");
+
+            let stale: Vec<_> = wlan_mlme::common::timer::make_async_timed_event_stream(
+                futures::stream::iter([original]),
+            )
+            .collect()
+            .await;
+            assert!(stale.is_empty(), "initial connect timer must be canceled");
+
+            mlme.handle_mac_frame_rx(
+                &peer_auth_frame(&fidl_mlme::SaeFrame {
+                    peer_sta_address: CLIENT,
+                    seq_num: 1,
+                    status_code: fidl_ieee80211::StatusCode::from_primitive_allow_unknown(77),
+                    sae_fields: vec![20, 0],
+                }),
+                rx_info(),
+                9.into(),
+            )
+            .await;
+            assert_eq!(mlme.sae_state_name(), "authenticating");
+            assert!(matches!(
+                effects.lock().unwrap().events.last(),
+                Some(fidl_mlme::MlmeEvent::OnSaeFrameRx { .. })
+            ));
+
+            mlme.handle_timeout(refreshed.1.event).await;
+            assert_eq!(mlme.sae_state_name(), "joined");
+        });
+}
diff -ruN a/src/connectivity/wlan/lib/mlme/rust/src/client/mod.rs b/src/connectivity/wlan/lib/mlme/rust/src/client/mod.rs
--- a/src/connectivity/wlan/lib/mlme/rust/src/client/mod.rs	2026-08-11 16:02:35.844224918 +0000
+++ b/src/connectivity/wlan/lib/mlme/rust/src/client/mod.rs	2026-08-11 16:02:35.840719513 +0000
@@ -333,6 +333,14 @@
 }
 
 impl<D> ClientMlme<D> {
+    pub fn sae_state_name(&self) -> &'static str {
+        self.sta
+            .as_ref()
+            .and_then(|sta| sta.state.as_ref())
+            .map(|state| state.sae_state_name())
+            .unwrap_or("station_absent")
+    }
+
     pub fn seq_mgr(&mut self) -> &mut SequenceManager {
         &mut self.ctx.seq_mgr
     }
diff -ruN a/src/connectivity/wlan/lib/mlme/rust/src/client/state.rs b/src/connectivity/wlan/lib/mlme/rust/src/client/state.rs
--- a/src/connectivity/wlan/lib/mlme/rust/src/client/state.rs	2026-08-11 16:02:35.844616148 +0000
+++ b/src/connectivity/wlan/lib/mlme/rust/src/client/state.rs	2026-08-11 16:03:23.181490944 +0000
@@ -199,6 +199,15 @@
     ) -> AuthProgress {
         let state =
             self.algorithm.handle_sme_sae_tx(sta, tx.seq_num, tx.status_code, &tx.sae_fields[..]);
+        crate::sae_disposition(format_args!(
+            "result=akm_sme_sae_tx category={}",
+            match &state {
+                Ok(akm::AkmState::AuthComplete) => "complete",
+                Ok(akm::AkmState::InProgress) => "in_progress",
+                Ok(akm::AkmState::Failed) => "failed",
+                Err(_) => "send_error",
+            }
+        ));
         self.akm_state_update_notify_sme(sta, state).await
     }
 
@@ -1308,11 +1317,58 @@
                     AuthProgress::InProgress => state.into(),
                     AuthProgress::Failed => state.transition_to(Joined).into(),
                 },
-                MlmeReq::SaeFrameTx(frame) => match state.on_sme_sae_tx(sta, frame).await {
-                    AuthProgress::Complete => state.transition_to(Associating::default()).into(),
-                    AuthProgress::InProgress => state.into(),
-                    AuthProgress::Failed => state.transition_to(Joined).into(),
-                },
+                MlmeReq::SaeFrameTx(frame) => {
+                    let peer_match = frame.peer_sta_address == sta.sta.bssid().to_array();
+                    let timeout_present = sta.sta.connect_timeout.is_some();
+                    crate::sae_disposition(format_args!(
+                        "gate=sme_sae_frame_tx state=authenticating peer_match={peer_match} timeout_present={timeout_present}"
+                    ));
+                    if !peer_match || !timeout_present {
+                        crate::sae_disposition(format_args!(
+                            "drop=sme_sae_frame_tx_unbound state=authenticating"
+                        ));
+                        return state.into();
+                    }
+                    let progress = state.on_sme_sae_tx(sta, frame).await;
+                    crate::sae_disposition(format_args!(
+                        "result=sme_sae_frame_tx progress={}",
+                        match progress {
+                            AuthProgress::Complete => "complete",
+                            AuthProgress::InProgress => "in_progress",
+                            AuthProgress::Failed => "failed",
+                        }
+                    ));
+                    match progress {
+                        AuthProgress::Complete => {
+                            crate::sae_disposition(format_args!(
+                                "next_state=associating reason=auth_complete"
+                            ));
+                            state.transition_to(Associating::default()).into()
+                        }
+                        AuthProgress::InProgress => {
+                            let old_timeout = sta.sta.connect_timeout.take();
+                            drop(old_timeout);
+                            let duration = sta.sta.beacon_period()
+                                * sta.sta.connect_req.connect_failure_timeout;
+                            let timeout =
+                                sta.ctx.timer.schedule_after(duration, TimedEvent::Connecting);
+                            sta.sta.connect_timeout.replace(timeout);
+                            crate::sae_disposition(format_args!(
+                                "transition=sme_sae_frame_tx_committed before=authenticating after=authenticating old_timer=canceled new_timer=scheduled"
+                            ));
+                            crate::sae_disposition(format_args!(
+                                "next_state=authenticating reason=auth_in_progress"
+                            ));
+                            state.into()
+                        }
+                        AuthProgress::Failed => {
+                            crate::sae_disposition(format_args!(
+                                "next_state=joined reason=auth_failed"
+                            ));
+                            state.transition_to(Joined).into()
+                        }
+                    }
+                }
                 MlmeReq::Deauthenticate(_) => {
                     state.on_sme_deauthenticate(sta).await;
                     state.transition_to(Joined).into()
@@ -1396,7 +1452,7 @@
         }
     }
 
-    fn sae_state_name(&self) -> &'static str {
+    pub(crate) fn sae_state_name(&self) -> &'static str {
         match self {
             States::Joined(_) => "joined",
             States::Authenticating(_) => "authenticating",
