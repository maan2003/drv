diff -ruN a/src/connectivity/wlan/lib/sme/src/client/mod.rs b/src/connectivity/wlan/lib/sme/src/client/mod.rs
--- a/src/connectivity/wlan/lib/sme/src/client/mod.rs
+++ b/src/connectivity/wlan/lib/sme/src/client/mod.rs
@@ -82,11 +82,20 @@
     cfg: Config,
     pub wpa3_supported: bool,
     pub owe_supported: bool,
+    /// Host validation may extend only the initial RSNA response timer so a
+    /// passive observer can own its promised interval. Production defaults to
+    /// the unchanged 4000 ms SME timer.
+    pub initial_rsna_response_timeout: Option<zx::MonotonicDuration>,
 }
 
 impl ClientConfig {
     pub fn from_config(cfg: Config, wpa3_supported: bool, owe_supported: bool) -> Self {
-        Self { cfg, wpa3_supported, owe_supported }
+        Self {
+            cfg,
+            wpa3_supported,
+            owe_supported,
+            initial_rsna_response_timeout: None,
+        }
     }
 
     /// Converts a given BssDescription into a ScanResult.
diff -ruN a/src/connectivity/wlan/lib/sme/src/client/state/link_state.rs b/src/connectivity/wlan/lib/sme/src/client/state/link_state.rs
--- a/src/connectivity/wlan/lib/sme/src/client/state/link_state.rs
+++ b/src/connectivity/wlan/lib/sme/src/client/state/link_state.rs
@@ -140,6 +140,7 @@
     pub fn new(
         protection: Protection,
         context: &mut Context,
+        initial_rsna_response_timeout: Option<zx::MonotonicDuration>,
     ) -> Result<Self, EstablishRsnaFailureReason> {
         match protection {
             Protection::Open | Protection::Wep(_) => {
@@ -159,7 +160,15 @@
                     rsna_completion_timeout: Some(
                         context.timer.schedule(event::RsnaCompletionTimeout),
                     ),
-                    rsna_response_timeout: Some(context.timer.schedule(event::RsnaResponseTimeout)),
+                    rsna_response_timeout: Some(match initial_rsna_response_timeout {
+                        Some(timeout) => context
+                            .timer
+                            .schedule_after(
+                                timeout,
+                                Event::RsnaResponseTimeout(event::RsnaResponseTimeout),
+                            ),
+                        None => context.timer.schedule(event::RsnaResponseTimeout),
+                    }),
                     rsna_retransmission_timeout: None,
                     handshake_complete: false,
                     pending_key_ids: Default::default(),
diff -ruN a/src/connectivity/wlan/lib/sme/src/client/state/mod.rs b/src/connectivity/wlan/lib/sme/src/client/state/mod.rs
--- a/src/connectivity/wlan/lib/sme/src/client/state/mod.rs
+++ b/src/connectivity/wlan/lib/sme/src/client/state/mod.rs
@@ -453,7 +453,11 @@
                 }
 
                 // TODO(https://fxbug.dev/359842400) Check that peer_sta_address matches request.
-                match LinkState::new(self.cmd.protection, context) {
+                match LinkState::new(
+                    self.cmd.protection,
+                    context,
+                    self.cfg.initial_rsna_response_timeout,
+                ) {
                     Ok(link_state) => link_state,
                     Err(failure_reason) => {
                         let msg = "Connect terminated; failed to initialize LinkState".to_string();
@@ -1978,7 +1982,11 @@
             let wmm_param = parse_wmm_from_ies(&result_fields.association_ies);
 
             // Get started with link state before going to Associated.
-            let link_state = match LinkState::new(state.cmd.protection, context) {
+            let link_state = match LinkState::new(
+                state.cmd.protection,
+                context,
+                state.cfg.initial_rsna_response_timeout,
+            ) {
                 Ok(link_state) => link_state,
                 Err(failure_reason) => {
                     let disconnect_info = make_roam_disconnect_info(mlme_event_name, None);
