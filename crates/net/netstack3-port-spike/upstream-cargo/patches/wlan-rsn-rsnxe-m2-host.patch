diff -ruN a/src/connectivity/wlan/lib/rsn/src/key/exchange/handshake/fourway/mod.rs b/src/connectivity/wlan/lib/rsn/src/key/exchange/handshake/fourway/mod.rs
--- a/src/connectivity/wlan/lib/rsn/src/key/exchange/handshake/fourway/mod.rs	2026-09-07 05:48:44.004717781 +0000
+++ b/src/connectivity/wlan/lib/rsn/src/key/exchange/handshake/fourway/mod.rs	2026-09-07 05:48:44.057520003 +0000
@@ -219,6 +219,10 @@
     pub nonce_rdr: Arc<NonceReader>,
     pub gtk_provider: Option<Arc<Mutex<GtkProvider>>>,
     pub igtk_provider: Option<Arc<Mutex<IgtkProvider>>>,
+    /// RSNXE the supplicant sent in its (Re)Association Request, as the raw
+    /// element bytes. IEEE Std 802.11-2020, 12.7.6.3 requires message 2/4 to
+    /// repeat it; authenticators such as hostapd disconnect on a mismatch.
+    pub s_rsnxe: Option<Vec<u8>>,
 
     // Private field to ensure Config can only be created by one of Config's
     // associated functions.
@@ -300,6 +304,7 @@
             nonce_rdr,
             gtk_provider,
             igtk_provider,
+            s_rsnxe: None,
             _private: (),
         })
     }
@@ -312,6 +317,7 @@
             && self.s_protection == other.s_protection
             && self.a_addr == other.a_addr
             && self.a_protection == other.a_protection
+            && self.s_rsnxe == other.s_rsnxe
     }
 }
 
diff -ruN a/src/connectivity/wlan/lib/rsn/src/key/exchange/handshake/fourway/supplicant.rs b/src/connectivity/wlan/lib/rsn/src/key/exchange/handshake/fourway/supplicant.rs
--- a/src/connectivity/wlan/lib/rsn/src/key/exchange/handshake/fourway/supplicant.rs	2026-09-07 05:48:44.014494651 +0000
+++ b/src/connectivity/wlan/lib/rsn/src/key/exchange/handshake/fourway/supplicant.rs	2026-09-07 05:48:44.060048245 +0000
@@ -60,6 +60,11 @@
 
     let mut w = kde::Writer::new();
     w.write_protection(&cfg.s_protection)?;
+    // IEEE Std 802.11-2020, 12.7.6.3: the RSNXE, if the Supplicant included one
+    // in its (Re)Association Request, follows the RSNE in the Key Data field.
+    if let Some(rsnxe) = &cfg.s_rsnxe {
+        w.write_raw_element(rsnxe)?;
+    }
     let key_data = w.finalize_for_plaintext()?.into();
 
     let msg2 = eapol::KeyFrameTx::new(
diff -ruN a/src/connectivity/wlan/lib/rsn/src/key_data/kde.rs b/src/connectivity/wlan/lib/rsn/src/key_data/kde.rs
--- a/src/connectivity/wlan/lib/rsn/src/key_data/kde.rs	2026-09-07 05:48:44.023440196 +0000
+++ b/src/connectivity/wlan/lib/rsn/src/key_data/kde.rs	2026-09-07 05:48:44.062442444 +0000
@@ -228,6 +228,14 @@
         }
     }
 
+    /// Append one complete, already-serialized information element.
+    pub fn write_raw_element(&mut self, element: &[u8]) -> Result<(), BufferTooSmall> {
+        if !self.buf.can_append(element.len()) {
+            return Err(BufferTooSmall);
+        }
+        self.buf.append_bytes(element)
+    }
+
     fn write_kde_hdr(&mut self, hdr: Header) -> Result<(), BufferTooSmall> {
         if !self.buf.can_append(HDR_LEN) {
             return Err(BufferTooSmall);
diff -ruN a/src/connectivity/wlan/lib/rsn/src/lib.rs b/src/connectivity/wlan/lib/rsn/src/lib.rs
--- a/src/connectivity/wlan/lib/rsn/src/lib.rs	2026-09-07 05:48:44.031400106 +0000
+++ b/src/connectivity/wlan/lib/rsn/src/lib.rs	2026-09-07 05:48:44.067396523 +0000
@@ -83,6 +83,28 @@
         a_addr: MacAddr,
         a_protection: ProtectionInfo,
     ) -> Result<Supplicant, anyhow::Error> {
+        Self::new_wpa_personal_with_rsnxe(
+            nonce_rdr,
+            auth_cfg,
+            s_addr,
+            s_protection,
+            None,
+            a_addr,
+            a_protection,
+        )
+    }
+
+    /// Like `new_wpa_personal`, additionally carrying the raw RSNXE the station
+    /// sent in its (Re)Association Request so message 2/4 can repeat it.
+    pub fn new_wpa_personal_with_rsnxe(
+        nonce_rdr: Arc<nonce::NonceReader>,
+        auth_cfg: auth::Config,
+        s_addr: MacAddr,
+        s_protection: ProtectionInfo,
+        s_rsnxe: Option<Vec<u8>>,
+        a_addr: MacAddr,
+        a_protection: ProtectionInfo,
+    ) -> Result<Supplicant, anyhow::Error> {
         let negotiated_protection = NegotiatedProtection::from_protection(&s_protection)?;
         let gtk_exch_cfg = Some(exchange::Config::GroupKeyHandshake(group_key::Config {
             role: Role::Supplicant,
@@ -94,20 +116,22 @@
             auth::Config::ComputedPsk(psk) => Some(psk.to_vec()),
             _ => None,
         };
+        let mut fourway_cfg = fourway::Config::new(
+            Role::Supplicant,
+            s_addr,
+            s_protection,
+            a_addr,
+            a_protection,
+            nonce_rdr,
+            None,
+            None,
+        )?;
+        fourway_cfg.s_rsnxe = s_rsnxe;
         let esssa = EssSa::new(
             Role::Supplicant,
             pmk,
             negotiated_protection,
-            exchange::Config::FourWayHandshake(fourway::Config::new(
-                Role::Supplicant,
-                s_addr,
-                s_protection,
-                a_addr,
-                a_protection,
-                nonce_rdr,
-                None,
-                None,
-            )?),
+            exchange::Config::FourWayHandshake(fourway_cfg),
             gtk_exch_cfg,
         )?;
 
