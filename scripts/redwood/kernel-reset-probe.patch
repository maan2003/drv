diff --git a/drivers/firmware/psci/psci.c b/drivers/firmware/psci/psci.c
index e73bae6cb..1e01a01a9 100644
--- a/drivers/firmware/psci/psci.c
+++ b/drivers/firmware/psci/psci.c
@@ -17,6 +17,7 @@
 #include <linux/printk.h>
 #include <linux/psci.h>
 #include <linux/reboot.h>
+#include <linux/kmsg_dump.h>
 #include <linux/slab.h>
 #include <linux/suspend.h>
 
@@ -309,6 +310,9 @@ static int get_set_conduit_method(const struct device_node *np)
 static int psci_sys_reset(struct notifier_block *nb, unsigned long action,
 			  void *data)
 {
+	pr_emerg("REDWOOD_RESET_PROBE: before PSCI reset\n");
+	dump_stack();
+	kmsg_dump_desc(KMSG_DUMP_EMERG, "REDWOOD_RESET_PROBE: before PSCI reset");
 	if ((reboot_mode == REBOOT_WARM || reboot_mode == REBOOT_SOFT) &&
 	    psci_system_reset2_supported) {
 		/*
@@ -331,6 +335,9 @@ static struct notifier_block psci_sys_reset_nb = {
 
 static void psci_sys_poweroff(void)
 {
+	pr_emerg("REDWOOD_RESET_PROBE: before PSCI poweroff\n");
+	dump_stack();
+	kmsg_dump_desc(KMSG_DUMP_EMERG, "REDWOOD_RESET_PROBE: before PSCI poweroff");
 	invoke_psci_fn(PSCI_0_2_FN_SYSTEM_OFF, 0, 0, 0);
 }
 
diff --git a/drivers/power/reset/msm-poweroff.c b/drivers/power/reset/msm-poweroff.c
index c7eb6dc8e..0afb77f81 100644
--- a/drivers/power/reset/msm-poweroff.c
+++ b/drivers/power/reset/msm-poweroff.c
@@ -11,12 +11,16 @@
 #include <linux/platform_device.h>
 #include <linux/module.h>
 #include <linux/reboot.h>
+#include <linux/kmsg_dump.h>
 #include <linux/pm.h>
 
 static void __iomem *msm_ps_hold;
 
 static int do_msm_poweroff(struct sys_off_data *data)
 {
+	pr_emerg("REDWOOD_RESET_PROBE: before PS_HOLD write\n");
+	dump_stack();
+	kmsg_dump_desc(KMSG_DUMP_EMERG, "REDWOOD_RESET_PROBE: before PS_HOLD write");
 	writel(0, msm_ps_hold);
 	mdelay(10000);
 
diff --git a/fs/pstore/ram.c b/fs/pstore/ram.c
index 2eb0d4fa2..215886045 100644
--- a/fs/pstore/ram.c
+++ b/fs/pstore/ram.c
@@ -675,7 +675,8 @@ static int ramoops_parse_dt(struct platform_device *pdev,
 	if (of_property_read_bool(of_node, "no-dump-oops"))
 		pdata->max_reason = KMSG_DUMP_PANIC;
 	else
-		pdata->max_reason = KMSG_DUMP_OOPS;
+		/* Test kernel: retain synchronous reset-path snapshots. */
+		pdata->max_reason = KMSG_DUMP_EMERG;
 
 #define parse_u32(name, field, default_value) {				\
 		ret = ramoops_parse_dt_u32(pdev, name, default_value,	\
diff --git a/kernel/reboot.c b/kernel/reboot.c
index 695c33e75..121ab7484 100644
--- a/kernel/reboot.c
+++ b/kernel/reboot.c
@@ -91,6 +91,9 @@ static BLOCKING_NOTIFIER_HEAD(reboot_notifier_list);
  */
 void emergency_restart(void)
 {
+	pr_emerg("REDWOOD_RESET_PROBE: emergency_restart\n");
+	dump_stack();
+	kmsg_dump_desc(KMSG_DUMP_EMERG, "REDWOOD_RESET_PROBE: emergency_restart");
 	kmsg_dump(KMSG_DUMP_EMERG);
 	system_state = SYSTEM_RESTART;
 	machine_emergency_restart();
@@ -286,6 +289,9 @@ static void do_kernel_restart_prepare(void)
  */
 void kernel_restart(char *cmd)
 {
+	pr_emerg("REDWOOD_RESET_PROBE: kernel_restart\n");
+	dump_stack();
+	kmsg_dump_desc(KMSG_DUMP_EMERG, "REDWOOD_RESET_PROBE: kernel_restart");
 	kernel_restart_prepare(cmd);
 	do_kernel_restart_prepare();
 	migrate_to_reboot_cpu();
@@ -704,6 +710,9 @@ EXPORT_SYMBOL_GPL(kernel_can_power_off);
  */
 void kernel_power_off(void)
 {
+	pr_emerg("REDWOOD_RESET_PROBE: kernel_power_off\n");
+	dump_stack();
+	kmsg_dump_desc(KMSG_DUMP_EMERG, "REDWOOD_RESET_PROBE: kernel_power_off");
 	kernel_shutdown_prepare(SYSTEM_POWER_OFF);
 	do_kernel_power_off_prepare();
 	migrate_to_reboot_cpu();
--- a/arch/arm64/kernel/traps.c
+++ b/arch/arm64/kernel/traps.c
@@ -939,6 +939,19 @@
 	if (regs)
 		__show_regs(regs);
 
+	/* Test-only: an SError delivered on SVC entry otherwise hides EL0 state.
+	 * Read only the current task's saved frame, never user memory or VMAs.
+	 */
+	if (!(current->flags & PF_KTHREAD)) {
+		struct pt_regs *user_regs = task_pt_regs(current);
+
+		if (user_mode(user_regs)) {
+			pr_crit("REDWOOD_RESET_PROBE: saved EL0 context syscall=%d orig_x0=%016llx\n",
+				user_regs->syscallno, user_regs->orig_x0);
+			__show_regs(user_regs);
+		}
+	}
+
 	nmi_panic(regs, "Asynchronous SError Interrupt");
 
 	cpu_park_loop();
