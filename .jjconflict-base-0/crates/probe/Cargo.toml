[package]
name = "drv-probe"
version.workspace = true
edition = "2021"
license.workspace = true

[dependencies]
wit-bindgen-rt = { version = "0.44.0", features = ["bitflags"] }

[lib]
crate-type = ["cdylib"]

[package.metadata.component]
package = "drv:hardware"

[package.metadata.component.target]
path = "../../wit"
world = "driver"
